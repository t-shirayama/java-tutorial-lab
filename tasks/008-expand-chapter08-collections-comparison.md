# タスク: 8章にコレクション使い分け比較表と失敗例を追加する

## 背景

List/Map/Set/Deque/配列を扱っているが、各コレクションの使いどころ比較が不足している。

## 対象

- `docs/08-collections-and-arrays/README.md`
- `docs/08-collections-and-arrays/examples/`

## 修正内容

- List、Map、Set、Deque、配列の比較表を追加する。
- `List.of()`に`add()`して例外になる失敗例を追加する。
- Level 1〜3の章末演習を追加する。

## 完了条件

- 目的に応じてコレクションを選べる。
- 変更不可コレクションの注意点が実行例で分かる。
