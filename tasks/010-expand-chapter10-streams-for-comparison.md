# タスク: 10章にfor文との比較でストリームを説明する

## 背景

ストリーム章は項目紹介に近く、便利さが伝わりにくい。

## 対象

- `docs/10-streams/README.md`
- `docs/10-streams/examples/`

## 修正内容

- for文での集計例とStreamでの集計例を並べる。
- `filter`、`map`、`toList`、`count`、`sum`を具体例で説明する。
- `Optional.get()`を安易に呼ぶ失敗例を追加する。
- 並列ストリームで副作用に注意する説明を追加する。

## 完了条件

- ストリームの生成、中間処理、終端処理を説明できる。
- for文から置き換える理由と注意点が分かる。
