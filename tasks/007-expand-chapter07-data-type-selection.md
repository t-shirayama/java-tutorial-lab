# タスク: 7章にrecord/class/enum/sealedの使い分けを追加する

## 背景

`record`、`enum`、`sealed`を扱っているが、初心者には普通のクラスとの使い分けが不足している。

## 対象

- `docs/07-data/README.md`
- `docs/07-data/examples/`

## 修正内容

- `record`と通常クラスの比較表を追加する。
- `enum`を文字列定数より優先する理由を失敗例つきで説明する。
- `sealed`は種類が固定されているデータに向くことを補足する。

## 完了条件

- どのデータ表現を選ぶべきかの判断材料がある。
- Java 21の説明として正確である。
